## [1.1.2] - [2026-09-11]

### 变更

- **时光轴再改版**：上一版那条 1px 的竖轴在浅底上几乎看不见，一行里也空得只剩两行字。现在每条记录是一张卡片 —— 72px 封面缩略图、标题、日期、倒计时；轴加粗到 2px，每张卡在轴上按一枚结点，一眼看得出卡片挂在哪天
- 时光轴的年份从「轴边一小段刻度」换成左对齐的**分节标题**（`2027 · 3`，右边一条横线拉到屏幕边）；卡片上只写月日，年份不重复说第二遍
- 概览带从一条 56px 的点带换成一把 64px 的**时间尺**：3px 轨道、8px 记录点、一整块跟着滚动走的高亮、一年一枚刻度（挤得下就写年份），「今天」是一枚带标签的指针

### 修复

- 时光轴卡片在窄屏（320dp）上，日期和倒计时会溢出卡片边框；现在倒计时撑满余下的宽度、右对齐，放不下时收成省略号

## [1.1.1] - [2026-09-11]

### 变更

- **详情页**：一个画面里「就是今天」只说一次 —— 保留右上角那枚白徽章；中间那句改成「从今天开始」（只影响起始日就是今天的记录，以前会摆一串 00年00月00日），右下角不再重复第三遍
- **详情页右下角**：倒计时换成「重复规则 · 提醒」状态行（`每年 · 提前3天 09:00` / `不重复 · 不提醒`）—— 这两件事以前只有编辑页看得到，而点开一条记录最想确认的恰恰是「它会不会提醒我」。倒计时本身收进中区，并补上「还有 / 已经」的说法
- **分享海报**：一页一张，左右滑动切换记录，打开时停在离今天最近的那张；底部用圆点标出位置（超过 8 条换成页码），保存与分享只作用于当前这张
- **关于页**：删掉「深色模式」开关 —— 首页弹层里已经有同一个开关，两个入口改起来还会互相打架
- 关于页页脚的「隐私协议 / 软件使用协议 / 官方地址」拆成三个真实链接（README 隐私章节 / LICENSE / 仓库首页），原先三个名字都指向仓库首页
- 关于页的紫弧与青色圆斑改用调色板 token，暗色下自动压饱和：原值叠在 `#121212` 上是一道刺眼的霓虹，而它们只是背景装饰
- **时光轴重做**：横轴（今天钉在正中、靠对数刻度压缩）换成**竖轴 + 概览带**。竖轴未来在上、今天一道横标、过去在下，一屏装不下就滚 —— 不再有「三十条以后只能靠缩放读」的天花板，每条记录也不必再把信息挤进 92px 宽的标签里，跨年处会自己长出一行年份刻度。顶上那条概览带把全部记录按**时间**摊成一条点带（疏密就是那些年真正攒下的东西，不是行号的均匀分布），当前看着的那一段跟着滚动高亮，点带上任意一处就跳过去；滑走之后标题栏才浮出「回到今天」，一屏装得下时整条带子不出现
- 时光轴的排版抽成定高的纯函数（`utils/timeline_layout.dart`）：概览带的高亮、点按跳转、「回到今天」的显隐，全部由「滚动位置 ↔ 行号」的精确映射算出，不靠逐帧测量
- README 新增「隐私」章节：数据存在哪、什么时候会离开手机，以及「不收集、不上传、不联网」的承诺

### 修复

- **详情页右下角的状态行在 360 宽的屏上被截成半句**：它和左边的日期各分到一半宽度（两个 `flex: 1`），`每年 · 提前3天 09:00` 放不下。现在状态行按自身宽度摆放，日期负责伸缩

## [1.1.0] - [2026-09-11]

### 新增

- **提醒通知**：提前 N 天 + 具体时刻，走精确闹钟；重启手机后自动重排；拒绝通知权限或被收回精确闹钟时降级并在设置里如实提示，不崩、不反复弹窗
- **通知栏常驻倒计时**：常驻一条静默通知显示最近的一条记录（默认关，可手动划掉 —— 不做「划不掉」的流氓行为）
- **重复规则**：每年 / 每月，月末自动夹取（2 月 29 日的每年重复在平年落到 2 月 28 日）。重复记录支持**双视角**：点按切换「自起始日的年月日」↔「距下一次还有几天」
- **时光轴**：独立页面，今天固定在屏幕正中；跨度大的记录用对数刻度压缩，远的不会挤成一条线
- **数据备份与恢复**：导出 zip（记录 + 全部照片），导入为**覆盖**式且需要二次确认；解压到临时目录、校验通过才替换，不会出现「清空了但没导进来」
- **亮 / 暗主题**：手动切换并持久化到 `settings.json`；不跟随系统。贴在照片上的 8 个文字样式永远是白色，不参与切换
- **隐私锁**：冷启动 / 后台超过 30 秒后进 App 需过系统验证（指纹或锁屏密码）。**是遮罩不是加密**，数据库与照片仍是明文，README 与设置项里的说法保持一致
- **全屏看大图**：照片封面点开可双指缩放（1×–4×）、双击切换、下滑退出
- **当天彩蛋**：记录落在今天时，卡片多一枚「就是今天」缎带与呼吸光晕，详情页进入时撒一次彩纸
- **手感层**：触感反馈（点击 / 切换 / 保存 / 删除）、首页卡片长按快捷菜单（编辑 / 海报 / 加入系统日历 / 删除）、卡片错落入场、详情页数字滚动、列表滚动视差、统一转场曲线
- **启动画面**：自绘 Lottie 圆弧 + 「时光」淡入，分层启动（原生纯色 → Flutter 首帧 → Lottie 覆盖层），数据就绪再淡出
- **一键模板**：表单顶部一排 chip（恋爱 / 生日 / 结婚 / 入职 / 考试倒计时），选中即预填标题、副标题、封面色、重复规则与提醒
- **加入系统日历**：长按菜单把一条记录导出成 `.ics`，让系统日历也提醒一次 —— 也是对国产 ROM 限制精确闹钟的实际兜底
- **桌面图标长按快捷方式**：「新增纪念日」「时光轴」

### 变更

- 桌面图标直接取 `assets/images/app.png` 原图铺满自适应图标可视区，不再有独立的白色圆角板与深色底（旧做法把图压小、四周多出一圈底色，且前景 PNG 的圆角处理破坏了 alpha 通道）
- 数据库 v2 → v3 → v4：新增 `repeatRule` 与提醒四列（共 12 列）。任意老版本都能逐级升上来
- 全 App 的「天」口径统一收敛到 `nextDate`：非重复记录 `nextDate == date`，所以首页卡片、详情页、海报三处的行为一个字都没改
- 首页改为监听 Repository 的 `ValueNotifier`，新增 / 编辑 / 删除 / 导入 / 通知点入全部自动刷新；跨零点回到前台自动重算，不再显示昨天的天数
- 空白态与加载态抽成共用组件；关于页与对话框的尺寸魔数收进 `dimens.dart`；补齐 `primaryButtonStyle` 等缺失 token

### 修复

- 详情页卡片顶部不再被状态栏与返回按钮压住（内容层新增 `contentTopInset`）
- 对话框按钮文字不再写死白色 —— 暗色下 `buttonPrimary` 本身已接近白，白字会看不见
- 首页从 FAB 菜单新增记录后返回，列表不刷新的问题
- **加入系统日历**：重复规则改用和 App 一致的「月末夹取」口径 —— 2 月 29 日的每年重复在平年落到 2 月 28 日（原先日历整年跳过，四年只响一次），每月 29/30/31 日在 2 月同样落到 28/29 日（原先整月跳过）；提醒开关关掉的记录导出后不再带闹钟
- **备份导出漏掉全部照片**：`coverKey` 带着 `f:` 前缀被直接丢给 `locate()`，路径拼成 `.../f:covers/xxx.jpg` 恒不存在 —— 导出的 zip 里一张照片都没有，换机恢复时全变渐变封面。同一根因还让表单取消、换封面、删除记录时都删不掉原文件（只能等冷启动 24 小时清扫）。现在统一在 `locate` 入口剥前缀
- **通知冷启动点按不跳转**：进程被系统杀掉之后点提醒，是靠 `getNotificationAppLaunchDetails()` 把这次点击带回来的，而它一直没被读过，App 只会停在首页
- **隐私锁打断正在填的表单**：相册选择器、备份文件选择器、分享面板都会把 App 压到后台，停留超过 30 秒回来直接清栈，表单连同已经拷进 `covers/` 的照片一起丢。这几类外部界面现在豁免上锁，另留 10 分钟兜底超时
- 首页卡片、时光轴圆点、表单封面按实际尺寸解码，不再为 15px 的圆点把 1600×1600 的原图整张读进内存
- 提醒重排串行化：两次重排交叉时，后启动的那次会拿着旧列表把已删记录的闹钟重新排上
- 数据库打开失败不再被永久缓存（一次偶发失败曾等于永久打不开库，界面静默空列表），下次读写会重试
- 锁页「先跳过」对比度从 1.8:1 提到 4.6:1 —— 验证失败时唯一的退路，读不出来是事故

### 权限

- 清单里从 1 条增加到 6 条：`POST_NOTIFICATIONS` / `SCHEDULE_EXACT_ALARM` / `RECEIVE_BOOT_COMPLETED` / `VIBRATE` / `USE_BIOMETRIC` / `WRITE_EXTERNAL_STORAGE`(maxSdk 29)
- 打包后合并结果是 9 条：依赖库另外带进来 `READ_EXTERNAL_STORAGE`(maxSdk 29)、`USE_FINGERPRINT`（旧声明）和 AndroidX 的 `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`（签名级，收紧广播接收器）
- **仍然没有 `INTERNET`**

### 从 2020 原版重做

- 改为**完全离线**：删除 YApi mock 后端与全部网络请求，不再申请 `INTERNET` 权限
- 迁移到 Dart 3 / Flutter 3.44，重建 Android 工程（AGP 9 / Gradle 9.1，minSdk 24 / targetSdk 36）
- 修复首页永久卡在加载动画的 bug（启动就去请求已失效的接口，异常逃出后 `loading` 永远停在 `true`）
- 修复年月日计数错误：改用「最大整数月 + 余数天」算法（2019-05-08 → 2020-09-10 由 `01年04月06天` 修正为 `01年04月02天`）
- 修复照片丢失：照片从 `image_picker` 缓存目录复制到应用文档目录 `covers/`，数据库改存相对文件名
- 数据库 v1 → v2：新增 `coverKey` 列，`imageUrl` 保留为历史存档，老记录读不到照片时回退渐变封面
- 首页卡片新增「已经 N 天 / 还有 N 天」，按日期自动分「未来 / 已过去」两组并排序
- 详情页计数由定时器自动轮播改为点按切换，删掉每秒一次的 `setState`；删除「点哪都退出」，换成左上角返回按钮
- 分享海报整个重做：文字改用代码绘制，输出 1080×1920，支持保存相册与系统分享
- 修复编辑 / 删除静默失效（`ConflictAlgorithm.replace` 且不检查影响行数）
- 删除数据库为空时在 `build()` 里凭空造出的 `id: 0` 幽灵「欢迎」卡片
- 删除 webview，关于页外部链接改由系统浏览器打开

---

## [1.0.0] - [2020-09-09]

- 完成第一版


## [0.3] - [2020-09-08]

- 完成编辑功能
- 完成删除功能
- 首次进入APP时，随机初始化一张背景图
- 体验优化


## [0.2] - [2020-09-07]

- 首页UI调整
- 悬浮按钮加动画效果
- 新增纪念日时，支持选择背景图片
- 新增 sqlflite 支持数据持久化

## [0.1] - [2020-09-01]

- 新增添加页面UI
- 完成日期选择器


## [0.0] - [2020-08-31]

- 配置APP LOGO
- 完成首页和详情页
- 样式优化



## [0.0] - [2020-08-23]

- 项目初始化
